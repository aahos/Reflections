What happens when you initialize a repository? Why do you need to do it?
When i initialize a repository, it creats a .git file which contains meta data for the newly created respository, which contains history and releted data for the files in the directory. To maintain different versions of the file in the directory.
